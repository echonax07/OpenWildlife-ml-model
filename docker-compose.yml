version: "3.9"

networks:
  labelstudio_shared:
    external: true
services:
  # ----- MMDetection backend -----
  mmwhale2:
    image: mmwhale2:latest
    container_name: mmdetection
    init: true
    networks:
      - labelstudio_shared
    restart: unless-stopped
    ports:
      - "9090:9090"
    environment:
      - BASIC_AUTH_USER=
      - BASIC_AUTH_PASS=
      - LOG_LEVEL=DEBUG
      - WORKERS=1
      - THREADS=8
      - MODEL_DIR=/data/models
      - checkpoint_file=/app/work_dirs/mm_grounding_dino_real_filtered_epoch10/epoch_50.pth
      - labels_file=/app/work_dirs/mm_grounding_dino_real_filtered_epoch10/label_map.json
      - config_file=/app/configs/eider_ducks/mm_grounding_dino_real_filtered_epoch10.py
      - model_params_file=/app/work_dirs/mm_grounding_dino_real_filtered_epoch10/model_params.json
      - device=cuda
      - SCORE_THRESHOLD=0.3
      - LABEL_STUDIO_HOST=http://172.18.0.5:8085
      # - LABEL_STUDIO_HOST=http://ls_nginx:8085
      - LABEL_STUDIO_API_KEY=1827b001dbd5abe74cf64744683815d4f6394c20
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      # Time out in seconds
      - ML_TIMEOUT_TRAIN= 1000
      - ML_TIMEOUT_PREDICT= 300
    volumes:
      - ./data/server:/data
      - ./work_dirs:/app/work_dirs
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1  # Use 1 GPU; set to 'all' to use all available GPUs
              capabilities: [ gpu ]
# Start redis server

# Start RQ server
